
function selectionSort(array) {

  // declare a temp variable, 'min' to hold a single integer
  // declare a temp variable to count the re-assignments of values count to 'min' during each loop traverse of the array
  // find the integer in array index pos 0 and assign it's value to 'min'
  // traverse the remainder of the array and find out if any integer is lower than 'min
  // if is assign it's value to 'min'

};